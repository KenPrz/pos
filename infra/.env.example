# Copy to infra/.env for local development.
# Every key present, every secret blank — a missing variable should be a diff,
# not a discovery. See docs/04-backend-conventions.md.

POSTGRES_DB=pos
POSTGRES_USER=pos
POSTGRES_PASSWORD=pos
POSTGRES_PORT=5432
